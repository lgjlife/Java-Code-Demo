<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mybatis.user.dao.mapper.UserMapper" >
  <resultMap id="BaseResultMap" type="com.mybatis.user.dao.model.User" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="id" property="id" jdbcType="BIGINT" />
    <result column="name" property="name" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="insert" parameterType="com.mybatis.user.dao.model.User" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into user (id, name)
    values (#{id,jdbcType=BIGINT}, #{name,jdbcType=VARCHAR})
  </insert>
  <select id="selectAll" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, name
    from user
  </select>

  <!--<insert id="insertList" useGeneratedKeys="true" keyProperty="id">
    insert into user (name)
    values
    <foreach collection="list" item="user" separator=",">
     (#{user.name,jdbcType=VARCHAR})
    </foreach>
  </insert>-->

  <insert id="insertList" useGeneratedKeys="true" keyProperty="id">
    <selectKey resultType="java.lang.Long" keyProperty="id" keyColumn="id" order="AFTER" >
      SELECT LAST_INSERT_ID()
    </selectKey>

    insert into user (name)
    values
    <foreach collection="list" item="user" separator=",">
      (#{user.name,jdbcType=VARCHAR})
    </foreach>
  </insert>

  <insert id="insert1" parameterType="com.mybatis.user.dao.model.User" >

    <selectKey resultType="java.lang.Long" keyProperty="id" keyColumn="id" order="AFTER" >
      SELECT LAST_INSERT_ID()
    </selectKey>

    insert into user (name)
    values (#{name,jdbcType=VARCHAR})
  </insert>




</mapper>